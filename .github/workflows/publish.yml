name: Upload to S3

on: [push]
jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: copy file to github repo
        shell: bash
        env:
          API_TOKEN_GITHUB: ${{ secrets.TOKEN }}
        run: |
          echo "carpeta antes de clonar"
          ls
          git clone https://.:${{ secrets.TOKEN }}@github.com//facephi/central-packages.git
          echo "carpeta despues de clonar"
          ls
          cd central-packages/
          cp ../Podfile Podfile
          git add .
          git commit -m "Automatic publish from github.com/project"
          git push 
          git tag automatic
          git push --tags
      
     